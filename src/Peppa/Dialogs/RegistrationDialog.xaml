<ContentDialog
    x:Class="piggy_bank_uwp.Dialogs.RegistrationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:piggy_bank_uwp.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Registration a new user"
    PrimaryButtonText="Registration"
    CloseButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">


    <StackPanel x:Name="MainPanel">
        <TextBox x:Name="RegistrationText" Header="Login"/>
        <ComboBox x:Name="Currency" Header="Currency" Margin="0 5 0 0" Width="{Binding ElementName=MainPanel, Path=ActualWidth}"/>
        <PasswordBox x:Name="Password" Header="Password" Margin="0 5 0 0"/>
        <PasswordBox x:Name="ConfirmPassword" Header="Confirm Password" Margin="0 5 0 0"/>
    </StackPanel>

</ContentDialog>
